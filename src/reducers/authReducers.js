import { action } from "../utils"
const BASE_URL = action.BASE_URL
const INITIAL_STATE = {
    usertoken :'',
    userId :'',
}

export default (state = INITIAL_STATE, action)=>{
    switch (action.type) {
        case action.Set:
            
            break;
    
        default:
            break;
    }
}
